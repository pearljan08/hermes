{
  "name": "my-monorepo",
  "private": true,
  "workspaces": [
    "packages/client/*",
    "packages/server/*"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "typescript": "^4.0.0"
  }
}